// Export types or variables that will be used by the component and its stories.
  // Delete this file if not needed for this component.
  /**
  * export const FORM_CONTROL_VARIANTS = ['foo', 'bar', 'baz'] as const;
  * export type FormControlVariants = (typeof FORM_CONTROL_VARIANTS)[number];
  */
  
